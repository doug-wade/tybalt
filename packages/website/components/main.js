import { defineComponent, html } from '@tybalt/core';

defineComponent({
    name: 'tybalt-main',
    shadowMode: 'open',
    render() {
        return html`<div>Tutorial coming here soon!</div>`;
    },
});
