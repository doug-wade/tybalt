{
  "name": "@gambit/website",
  "version": "0.0.1",
  "description": "The gambit documentation website",
  "main": "index.js",
  "private": true,
  "scripts": {
    "build": "mkdir -p lib/ && cp -r ../core/dist lib/gambit-core",
    "clean": "rimraf lib/ _site/",
    "serve": "eleventy --serve --port 8081",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Douglas Wade <douglas.b.wade@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "@11ty/eleventy": "^1.0.2"
  },
  "dependencies": {
    "@gambit/core": "^0.0.1",
    "@gambit/eleventy-plugin": "^0.0.1"
  }
}
