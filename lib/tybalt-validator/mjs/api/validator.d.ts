import type { ValidatorFunction, ValidationResults } from '../types';
declare const _default: (cb: ValidatorFunction) => {
    validate(value?: any): Promise<ValidationResults>;
};
export default _default;
